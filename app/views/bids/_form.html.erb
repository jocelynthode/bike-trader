<%= form_for([@auction, @auction.bids.build]) do |f| %>
    <p>
      <%= f.label :amount %><br>
      <%= f.text_field :amount %>
    </p>

    <p>
      <%= f.label :threshold %><br>
      <%= f.text_field :threshold %>
    </p>
    <p>
      <%= f.submit %>
    </p>
<% end %>